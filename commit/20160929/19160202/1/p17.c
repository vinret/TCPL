#include <stdio.h> 
int main()
{
    signed char z;
    unsigned char x;
    long int c;
    int v;
    float b;
    double n;
    printf("signed char size=%d\n",sizeof(z));
    printf("unsigned char size=%d\n",sizeof(x));
    printf("long int size=%d\n",sizeof(c));
    printf("int size=%d\n",sizeof(v));
    printf("float size=%d\n",sizeof(b));
    printf("duble size=%d\n",sizeof(n));
    return 0;
}

