#include<stdio.h>
int main（）
{
    char  c;
    int  i;
    float  x;
    double  z;

    printf("char   size=%d\n",sizeof(c));
    printf("int    size=%d\n",sizeof(i));
    printf("float  size=%d\n",sizeof(X));
    printf("double size=%d\n",sizeof(z)); 

    return 0;
}

