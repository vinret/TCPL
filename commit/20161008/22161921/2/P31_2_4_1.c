#include<stdio.h>
#include<math.h>
int  main()
{

   int x=2,a=3,b=4,c=6;
   int y=x*x*x+2*x*x+3*x+1;
   printf("y");
   int m=sqrt(fabs(x));
   int n=4*a/(b*c);
   scanf("%d,%d",&m,&n);
   if(m==n)
       printf("%d",m==n);
   else
       printf("%d",m!=n);
   
   return 0;
} 
            

       
