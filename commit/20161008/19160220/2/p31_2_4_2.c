#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{int x,a,b,c;
scanf("%d,%d,%d,%d",&x,&a,&b,&c);
sqrt(abs(x))!=(4*a)/(b*c);
printf("sqrt(abs(x))!=(4*a)/(b*c)=%d",sqrt(abs(x))!=(4*a)/(b*c));
return 0;}
