#include<stdio.h>

int fac_bit_count(int n);
int main()
{
	int n,i;
		scanf("%d,&n");
			for (i = 2, i <= n - 1; i++;)
					if (n%i == 0)
								return 0;
										else
													return -1;
													}
