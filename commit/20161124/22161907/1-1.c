#include<stdio.h>
int GCD_recursive(int m, int n);
int main()
{
   int m,n,i;
   if (m>=n)
      i==m;
   else i==n;
   for (;i>=1;i--)
     {  if (n%i==0)
       printf("%d\n", i);
     }
   return 0;
}  
